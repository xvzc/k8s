apiVersion: v1
kind: Pod
metadata:
  name: pod-resource-auto
spec:
  containers:
  - name: container
    image: kubetm/init
    resources:
      requests:
        memory: 1Gi
      limits:
        memory: 2Gi
